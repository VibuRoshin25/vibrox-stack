apiVersion: v1
kind: Service
metadata:
  name: vibrox-dns
spec:
  type: ClusterIP
  selector:
    app: vibrox-dns
  ports:
    - name: dns-udp
      port: 53
      targetPort: dns-udp
      protocol: UDP
    - name: dns-tcp
      port: 53
      targetPort: dns-tcp
      protocol: TCP
